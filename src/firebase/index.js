// archivo de union de ambos metodos, context e initialapp

import firebase, { isLogged } from './firebase';
import FirebaseContext from '../firebase/context';


export default firebase;

export { FirebaseContext, isLogged };